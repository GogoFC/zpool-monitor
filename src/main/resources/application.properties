spring.application.name=zpool-monitor

# Dev
spring.datasource.url=jdbc:mariadb://mariadb.irondesign.dev:3306/zpoolmonitor?createDatabaseIfNotExist=true&autoReconnect=true&sslMode=DISABLED&serverTimezone=Europe/Berlin&useUnicode=true&characterEncoding=UTF-8&connectTimeout=30000&socketTimeout=30000
spring.jpa.open-in-view=true

# Production
# spring.datasource.url=jdbc:mariadb://mariadb.irondesign.dev:3306/zpoolmonitor?createDatabaseIfNotExist=true&autoReconnect=true&sslMode=VERIFY_IDENTITY&serverTimezone=Europe/Berlin&useUnicode=true&characterEncoding=UTF-8&connectTimeout=30000&socketTimeout=30000
# spring.jpa.open-in-view=false


# Username and password
spring.datasource.username = malmo
spring.datasource.password = laSeLgat

# Keep the connection alive if idle for a long time (needed in production)
spring.datasource.tomcat.test-while-idle= true
spring.datasource.tomcat.validation-query= SELECT 1

# ===============================
# = JPA / HIBERNATE
# ===============================
# Use spring.jpa.properties.* for Hibernate native properties (the prefix is
# stripped before adding them to the entity manager).
# Show or not log for each sql query (Slows down the system, remove in production)
spring.jpa.show-sql = true

# Hibernate ddl auto (create, create-drop, update): with "update" the database
# schema will be automatically updated accordingly to java entities found in
# the project. Set to none if its up and running
#spring.jpa.hibernate.ddl-auto = update
#create
spring.jpa.hibernate.ddl-auto=update

spring.security.user.name=user
spring.security.user.password=AwTERiNU
